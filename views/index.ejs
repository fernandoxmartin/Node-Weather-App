<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>FXM WEATHER</title>
    <link rel="stylesheet" type="text/css" href="style.css" />
    <link
      href="https://fonts.googleapis.com/css?family=Oswald|Poppins|Roboto&display=swap"
      rel="stylesheet"
    />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
  </head>
  <body>
    <main>
      <div class="left">
        <div class="leftContainer">
          <h1>fxm weather</h1>
          <div class="main-info">
            <span class="temp"><%= weather.temp %> &deg;</span>
            <div class="sub-info">
              <span class="city"><%= weather.city %></span>
              <span class="current"><%= weather.date %></span>
            </div>
          </div>
        </div>
      </div>
      <div class="right">
        <div class="header">
          <form method="GET" action="/">
            <input
              id="input"
              class="search"
              type="text"
              name="city"
              placeholder="Search by name or zip code"
            />
            <button id="button" type="submit"></button>
          </form>
          <div class="error"></div>
        </div>
        <div class="rightnow">
          <h2>Weather Details</h2>
          <img
            class="icon"
            src="http://openweathermap.org/img/wn/<%= weather.icon %>@2x.png"
            alt="weather icon"
          />
          <span class="description"><%= weather.desc %></span>
        </div>
        <div class="details">
          <div class="info"><span>Humidity</span> <%= weather.hum %> %</div>
          <div class="info"><span>Wind</span> <%= weather.wind %> MPH</div>
          <div class="info"><span>Clouds</span> <%= weather.clouds %> %</div>
          <div class="info">
            <span>Sunrise</span> <%= weather.sunrise %> PST
          </div>
          <div class="info"><span>Sunset</span> <%= weather.sunset %> PST</div>
        </div>
      </div>
    </main>
  </body>
</html>
